/**
 * Handles the process of reading objects flagged as templates and generating
 * their copies as needed.
 *
 * This merely handles determining the values and returning them. The event handling
 * inquestion may use the values to overwrite an existing object or as details for
 * a new object.
 * @class Templating
 * @constructor
 * @static
 */
module.exports = new (function() {
	
})();
